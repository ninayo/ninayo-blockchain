<%= form_tag create_price_path, validate:true do %>
	<hr>
	<%=fields_for 'prices[]', @new_prices[0] do |price| %>
		<fieldset>
			<div class="field">
				<strong><%= I18n.locale == :en ? "Maize" : "Mahindi" %></strong>
				<span class="text-right"><%= number_to_currency(@maize_prices.first.price, precision: 0) %></td>
				<%= price.hidden_field :crop_type_id, :value => 1 %>
			</div>

			<div class="field">
				<div class="input-wrapper">
					<strong><%= price.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
					<div class="input-field">
						<%= price.number_field :price, :min => 1, :class => "is-inline", :style => "width: 100px;" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= price.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= price.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= price.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>
	<hr>
	<%=fields_for 'prices[]', @new_prices[1] do |price| %>
		<fieldset>
			<div class="field">
				<strong><%= I18n.locale == :en ? "Potatoes" : "Viazi" %></strong>
				<span class="text-right"><%= number_to_currency(@potato_prices.first.price, precision: 0) %></td>
				<%= price.hidden_field :crop_type_id, :value => 2 %>
			</div>

			<div class="field">
				<div class="input-wrapper">
					<strong><%= price.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
					<div class="input-field">
						<%= price.number_field :price, :min => 1, :class => "is-inline", :style => "width: 100px;" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= price.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= price.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= price.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>
	<hr>
	<%=fields_for 'prices[]', @new_prices[2] do |price| %>
		<fieldset>
			<div class="field">
				<strong><%= I18n.locale == :en ? "Avocado" : "Maparachichi" %></strong>
				<span class="text-right"><%= number_to_currency(@avocado_prices.first.price, precision: 0) %></td>
				<%= price.hidden_field :crop_type_id, :value => 6 %>
			</div>

			<div class="field">
				<div class="input-wrapper">
					<strong><%= price.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
					<div class="input-field">
						<%= price.number_field :price, :min => 1, :class => "is-inline", :style => "width: 100px;" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= price.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= price.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= price.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>
	<hr>
	<%=fields_for 'prices[]', @new_prices[3] do |price| %>
		<fieldset>
			<div class="field">
				<strong><%= I18n.locale == :en ? "Tomatoes" : "Nyanya" %></strong>
				<span class="text-right"><%= number_to_currency(@tomato_prices.first.price, precision: 0) %></td>
				<%= price.hidden_field :crop_type_id, :value => 8 %>
			</div>

			<div class="field">
				<div class="input-wrapper">
					<strong><%= price.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
					<div class="input-field">
						<%= price.number_field :price, :min => 1, :class => "is-inline", :style => "width: 100px;" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= price.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= price.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= price.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>
	<hr>
	<%=fields_for 'prices[]', @new_prices[4] do |price| %>
		<fieldset>
			<div class="field">
				<strong><%= I18n.locale == :en ? "Onions" : "Vitunguu" %></strong>
				<span class="text-right"><%= number_to_currency(@onion_prices.first.price, precision: 0) %></td>
				<%= price.hidden_field :crop_type_id, :value => 13 %>
			</div>

			<div class="field">
				<div class="input-wrapper">
					<strong><%= price.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
					<div class="input-field">
						<%= price.number_field :price, :min => 1, :class => "is-inline", :style => "width: 100px;" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= price.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= price.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= price.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>
	<hr>
	<%= submit_tag("Publish", :class => "button") %>

 <% end %>
