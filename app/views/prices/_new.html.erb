<%= form_tag price_path, validate:true do %>

	<%=fields_for prices[0], do |pp| %>
		<fieldset>
			<div class="field">
				<strong><%= pp.label :crop_type_id, "Potatoes", :class => 'field-label' %></strong>
				<%= ff.hiden_field :crop_type_id, 2 %>
			</div>

			<div class="field">
				<strong><%= pp.label :price, "#{t "views.ad.price"}", :class => 'field-label' %></strong>
				<div class="input-wrapper">
					<div class="input-field">
						<%= pp.number_field :price, :min => 1, :class => "is-inline" %>
					</div>
					<div class="input-label">
						<span class="input-post">/=</span>
					</div>
				</div>
			</div>
			<% if current_view =="dar" %>
				<%= pp.hidden_field :region_id, :value => 2 %>
			<% elsif current_view == "iringa" %>
				<%= pp.hidden_field :region_id, :value => 5 %>
			<% else %>
				<%= pp.hidden_field :region_id, :value => 13 %>
			<% end %>
		</fieldset>
	<% end %>

	<%= f.submit("Publish", :class => "button") %>

 <% end %>
