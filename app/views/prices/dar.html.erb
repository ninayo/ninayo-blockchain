<% provide(:title, "Current market prices: Dar es Salaam") %>
<%= render "pagenav", :current_view => "dar" %>
<div class="update-button" style="display: flex; justify-content: center;">
	<% if current_user && current_user.admin? %>
	<%= link_to("Update Prices", new_dar_price_path, :class => "button is-block") %>
	<% end %>
</div>
<div class="tabs-content">
	<%= render "dar", :current_view => "dar" %>
</div>
<% if I18n.locale == :en %>
	<div class="disclaimer-content" style="display: flex; justify-content: center;">
		<p style="text-align: center;"><small>Price information reflects daily, average market prices. These are intended to provide Ninayo users with approximate pricing guidance. <br> Actual market prices are subject to current trading conditions.</small></p>
	</div>
<% else %>
	<div class="disclaimer-content" style="display: flex; justify-content: center;">
		<p style="text-align: center;"><small>Hizi ni Taarifa za Bei zinazoonesha Wastani wa Bei za soko za kila siku. Bei hizi ni kwa ajili ya kuwasaidia watumiaji wa Ninayo kupata Muongozo wa Bei elekezi za soko kulingana na hali halisi ya soko na Mazingira ya Biashara</small></p>
	</div>
<% end %>


	<% if current_user && current_user.admin? %>
		<% if !@nut_prices.first.related_ads.empty? %>
		<% @nut_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>

	<% if current_user && current_user.admin? %>
	<% if !@wheat_prices.first.related_ads.empty? %>
		<% @wheat_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>


	<% if current_user && current_user.admin? %>
	<% if !@maize_prices.first.related_ads.empty? %>
		<% @maize_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>

	<% if current_user && current_user.admin? %>
	<% if !@bean_prices.first.related_ads.empty? %>
		<% @bean_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>

	<% if current_user && current_user.admin? %>
	<% if !@soy_prices.first.related_ads.empty? %>
		<% @soy_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>

	<% if current_user && current_user.admin? %>
	<% if !@rice_prices.first.related_ads.empty? %>
		<% @rice_prices.first.related_ads.first do |a| %>
			<b><%= t("views.ad.for_sale") if a.sell? %>
			<%= t("views.ad.buying") if a.buy? %>:</b> <%= link_to(a.title, ad_path(a)) %> <%= " - " + a.user.name + " - " + l(a.published_at, format: :short) + "(" + time_ago_in_words(a.published_at).downcase + (I18n.locale == :sw ? "" : " ago") + ")" %>
			<hr>
		<% end %>
		<% else %>
			<%= "No matches" %>
		<% end %>
	<% end %>