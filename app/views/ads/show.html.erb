<%= link_to '< Back', ads_path %>

<div class="ad">
	<h1><%= @ad.title %></h1>
	<div class="description">
		<%= simple_format(@ad.description) %>
	</div>
	<p>
		<strong>Price:</strong>
		<%= @ad.price.round(0) %>/=
	</p>
</div>