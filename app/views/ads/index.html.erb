<div class="posts-wrapper">
	<%= render "layouts/alert" %>
	<strong><%= (I18n.locale == :sw "NINAYO Sokoni" : "The NINAYO Marketplace") %></strong>
	<%= render "filter", :ads => @ads %>
	<%= render "list", :ads => @ads %>
	<%= paginate @ads %>
</div>

