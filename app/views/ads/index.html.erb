<div class="posts-wrapper">
	<%= render "layouts/alert" %>
	<h4><strong><font color="#4CAF50"><%= (I18n.locale == :sw ? " Bonyeza vitufe vifuatavyo kwa kuanza" : "Click here to begin") %> ⇩</font></strong></h4>
	<%= render "filter", :ads => @ads %>
	<%= render "list", :ads => @ads %>
	<%= paginate @ads %>
</div>

